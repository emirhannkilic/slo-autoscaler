apiVersion: v1
kind: Service
metadata:
  name: load-target
  labels:
    app: load-target
spec:
  type: NodePort
  selector:
    app: load-target
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
      protocol: TCP
